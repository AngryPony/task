<h2>Edit campsite</h2>

<%= form_for :campsite, url: camp_sites_path(@campsite), method: :patch, remote: true do |f| %>
  <% if @campsite.errors.any? %>
    <% @campsite.errors.full_messages.each do |msg| %>
      <div class="alert alert-danger"> <%=msg %></div>
    <%end %>
  <%end %>
  <p>
    Name <br>
    <%= f.text_field(:name, {:class => 'form-control'}) %>
  </p>

  <p>
    Description <br>
    <%= f.text_area(:description, {:class => 'form-control'}) %>
  </p>

  <div>
    <label>Region</label>
    <%= f.collection_select :country_id, Country.all, :id, :name,{},{class: "form-select"} %>
  </div>
  <br/>

  <div>
    <label>City</label>
    <%= f.collection_select :country_id, Country.all, :id, :name,{},{class: "form-select"} %>
  </div>
  <br/>

  <p>
    <%= f.submit({:class => 'btn btn-success'}) %>
  </p>
<% end %>